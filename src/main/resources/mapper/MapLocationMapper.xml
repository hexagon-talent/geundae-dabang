<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gundaero.alley.domain.map.dao.MapLocationDAO">

    <select id="findAllAsGeoJson" resultType="map">
        SELECT
        id,
        title,
        ST_AsGeoJSON(center_point) AS center_geojson,
        ST_AsGeoJSON(edge_points)  AS polygon_geojson
        FROM tour_location
        ORDER BY id ASC
    </select>

    <select id="findOneAsGeoJson" parameterType="long" resultType="map">
        SELECT
        id,
        title,
        ST_AsGeoJSON(center_point) AS center_geojson,
        ST_AsGeoJSON(edge_points)  AS polygon_geojson
        FROM tour_location
        WHERE id = #{id}
        LIMIT 1
    </select>
    <select id="findAllUploadStatusByUser" parameterType="long" resultType="map">
        SELECT
        tl.id    AS location_id,
        tl.title AS title,
        /* MySQL에서 EXISTS는 0/1을 반환 -> uploaded 별칭으로 넘김 */
        EXISTS (
        SELECT 1
        FROM user_tour_location utl
        WHERE utl.user_id          = #{userId}
        AND utl.tour_location_id = tl.id
        LIMIT 1
        ) AS uploaded
        FROM tour_location tl
        ORDER BY tl.id ASC
    </select>
</mapper>