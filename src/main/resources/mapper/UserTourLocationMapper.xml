<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gundaero.alley.domain.photo.dao.UserTourLocationDAO">

    <resultMap id="UserTourLocationMap" type="com.gundaero.alley.domain.photo.entity.UserTourLocation">
        <id     column="id"                property="id"/>
        <result column="user_id"           property="userId"/>
        <result column="tour_location_id"  property="tourLocationId"/>
        <result column="photo_url"         property="photoUrl"/>
        <result column="created_at"        property="createdAt"/>
        <result column="updated_at"        property="updatedAt"/>
    </resultMap>
    <resultMap id="PhotoRowMap" type="com.gundaero.alley.domain.photo.entity.PhotoRow">
        <result column="tour_location_id" property="tourLocationId"/>
        <result column="location_title"   property="locationTitle"/>
        <result column="photo_url"        property="photoUrl"/>
    </resultMap>

    <insert id="upsert" parameterType="com.gundaero.alley.domain.photo.entity.UserTourLocation">
        INSERT INTO user_tour_location (user_id, tour_location_id, photo_url)
        VALUES (#{userId}, #{tourLocationId}, #{photoUrl})
        ON DUPLICATE KEY UPDATE
        photo_url = VALUES(photo_url),
        updated_at = CURRENT_TIMESTAMP
    </insert>

    <select id="findOne" resultMap="UserTourLocationMap">
        SELECT id, user_id, tour_location_id, photo_url, created_at, updated_at
        FROM user_tour_location
        WHERE user_id = #{userId}
        AND tour_location_id = #{locationId}
        LIMIT 1
    </select>
    <select id="findAllByUserWithTitle" resultMap="PhotoRowMap">
        SELECT
        utl.tour_location_id,
        tl.title       AS location_title,
        utl.photo_url
        FROM user_tour_location utl
        JOIN tour_location tl
        ON tl.id = utl.tour_location_id
        WHERE utl.user_id = #{userId}
        AND utl.photo_url IS NOT NULL
    </select>

    <insert id="insertOrUpdateUserTourLocations">
        INSERT INTO user_tour_location (photo_url, user_id, tour_location_id)
        VALUES
        <foreach collection="tourLocationIds" item="locId" separator=",">
            (#{photoUrl}, #{userId}, #{locId})
        </foreach>
        ON DUPLICATE KEY UPDATE
        photo_url = VALUES(photo_url),
        updated_at = CURRENT_TIMESTAMP
    </insert>

</mapper>